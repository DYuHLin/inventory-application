<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title><%=title %></title>
</head>
<body>
    <%- include('./head.ejs')  %>

    <p><%=shoe.title %></p>
    <% if(shoe_instance.length > 0) { %>
        <p>Delete the following shoe instances before deleting this shoe.</p>
        <%shoe_instance.forEach(element => { %>
            <a href="<%= element.url %>"><%= element.status %></a>
            <p><%= element.restocking %></p>
        <% }) %>
    <% }else { %>
        <p>Are you sure you want to delete this shoe?</p>
        <form method="POST">
            <input type="hidden" name="shoeid" value="<%=shoe._id %>">
            <button type="submit">Delete</button>
        </form>
    <% }; %>
</body>
</html>